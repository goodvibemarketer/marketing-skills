{
  "name": "livermore-market-key",
  "private": true,
  "version": "0.1.0",
  "description": "Livermore Market Key — deterministic rule engine, ledger, and tracking tool",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  }
}
